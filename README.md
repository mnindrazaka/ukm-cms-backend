# UKM CMS Backend
Backend web service for UKM CMS

## Development

```bash
# install dependency
$ composer install

# migrate database
$ php artisan migrate

# seed database
$ php artisan db:seed

# run server
$ php artisan serve
```
